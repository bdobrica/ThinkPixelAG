apiVersion: batch/v1
kind: Job
metadata:
  name: thinkpixelag-migrate
spec:
  template:
    spec:
      containers:
        - name: migrate
          image: thinkpixelag:phase8
          imagePullPolicy: Never
