# Migration boundary

This directory will contain ordered PostgreSQL migrations and migration test
fixtures. Once a migration has been shared or released it is immutable; schema
corrections are made with a new forward migration.
