# Policy boundary

This directory will contain versioned OPA/Rego packages, typed policy schemas,
fixtures, and policy tests. Policies deny by default and implement the contract
defined in `docs/contracts/policy-decision.md`.

Promoted policy bundles are controlled artifacts; local source files are not an
authoritative production activation mechanism.
