# ThinkPixelAG Release-Candidate TODO

This is the chronological implementation checklist. Execute the first unchecked item whose dependencies are complete. An item is checked only after its acceptance evidence passes. Follow the coding-agent and commit protocol in `PLAN.md` after every completed implementation item.

Status notation: `[ ]` pending, `[x]` implemented and verified. Add completion metadata as `— completed YYYY-MM-DD, commit <sha>, evidence: <commands/artifacts>`.

## Phase 0 — Decisions, threats, and contracts

- [x] GOV-001 Create `docs/` and an ADR template with status, context, decision, alternatives, consequences, security, operations, and references sections. — completed 2026-08-10, commit `eddf453`, evidence: `docs/adr/README.md`, `docs/adr/template.md`
- [x] GOV-002 Write the system context, trust-boundary, data-flow, and component diagrams; identify trusted gateways, workers, IdP, OPA, PostgreSQL, Valkey, KMS, and evidence sink. — completed 2026-08-10, commit `eddf453`, evidence: `docs/architecture/system.md`
- [x] GOV-003 Produce a threat model covering spoofing, tenant confusion, confused deputy/delegation, policy tampering, stale revocation, resource races, replay, evidence tampering, and governance-admin compromise. — completed 2026-08-10, commit `eddf453`, evidence: `docs/security/threat-model.md`
- [x] GOV-004 Define the data-classification and redaction rules for tokens, objectives, inputs, policy data, decisions, audit events, logs, traces, and metrics. — completed 2026-08-10, commit `eddf453`, evidence: `docs/security/data-classification.md`
- [x] GOV-005 Record supported Go, PostgreSQL, OPA, Valkey, Kubernetes, OpenAPI, and image-platform versions and the upgrade/support policy. — completed 2026-08-10, commit `eddf453`, evidence: `docs/supported-versions.md`; exact tested pins are a Phase 1 gate
- [x] GOV-006 Formalize agent/version, run, reservation, settlement, policy, and revocation state machines including allowed actors and illegal transitions. — completed 2026-08-10, commit `eddf453`, evidence: `docs/contracts/domain-model.md`
- [x] GOV-007 Formalize resource units, numeric bounds, rounding, allocation equations, extension rules, exhaustion semantics, and settlement invariants. — completed 2026-08-10, commit `eddf453`, evidence: `docs/contracts/resource-accounting.md`
- [x] GOV-008 Define revocation scopes, epoch relationships, ordering, staleness classes, fail-closed rules, reconciliation, and lift semantics. — completed 2026-08-10, commit `eddf453`, evidence: `docs/contracts/revocation.md`
- [x] GOV-009 Define the typed OPA input/output schema, reason-code registry, constraint-narrowing semantics, decision TTL rules, and failure behavior. — completed 2026-08-10, commit `eddf453`, evidence: `docs/contracts/policy-decision.md`
- [x] GOV-010 Draft OpenAPI 3.1 paths and schemas for public lifecycle and trusted/admin APIs, including auth, pagination, limits, idempotency, errors, and event resumption. — completed 2026-08-10, commit `eddf453`, evidence: `api/openapi/thinkpixelag.yaml`
- [x] GOV-011 Define SLOs and capacity targets for API latency/availability, admission, policy evaluation, revocation propagation/freshness, outbox lag, and allocation contention. — completed 2026-08-10, commit `eddf453`, evidence: `docs/operations/slos.md`
- [x] GOV-012 Review Phase 0 artifacts against the source blueprint and close all ambiguous trust sources and invariants. — completed 2026-08-10, commit `eddf453`, evidence: `docs/phase-0-review.md`
- [x] GOV-013 Run documentation/schema validation and commit Phase 0 with updated plan/checklist evidence. — completed 2026-08-10, commit `eddf453`, evidence: Redocly CLI 2.3.0 lint passed; `git diff --check` passed

## Phase 1 — Engineering foundation

- [x] ENG-001 Initialize the Go module, pin the Go/toolchain version, and create `cmd`, `internal`, `api`, `policies`, `migrations`, `deploy`, and `test` package boundaries. — completed 2026-08-10, commit `5287791`, evidence: `go version`; `go mod edit -json`; `go test ./...`; boundary documentation and package compile checks
- [x] ENG-002 Add dependency policy, license/source checks, vulnerability scanning configuration, and reproducible tool pinning. — completed 2026-08-10, commit `45c422d`, evidence: dependency checker unit/integration tests; pinned `govulncheck` and `go-licenses` gates; `go test ./...`; `git diff --check`
- [x] ENG-003 Implement strict typed configuration with defaults, environment/flag loading, startup validation, secret-safe rendering, and unit tests. — completed 2026-08-10, commit `be384bc`, evidence: configuration unit/redaction tests; `go test -race ./...`; `go vet ./...`; dependency, vulnerability, and license gates
- [x] ENG-004 Implement structured logging with request/trace correlation and centralized sensitive-field redaction tests. — completed 2026-08-10, commit `f6c3a9e`, evidence: logging/config unit and recursive leak tests; `go test -race -cover ./...`; `go vet ./...`; source/license/vulnerability gates
- [x] ENG-005 Add Prometheus metrics and OpenTelemetry tracing initialization with no-op/local configurations. — completed 2026-08-10, commit `215bfbf`, evidence: Prometheus exposition/no-op/cardinality tests; OpenTelemetry no-op/OTLP export/propagation/shutdown tests; race/vet/source/license/vulnerability gates
- [x] ENG-006 Add ID, UTC clock, decimal/resource quantity, pagination cursor, and typed error primitives with tests/fuzz cases. — completed 2026-08-10, commit `8ff8f77`, evidence: deterministic primitive/overflow/tamper tests; three fuzz campaigns; `go test -race -cover ./...`; vet/source/license/vulnerability gates
- [x] ENG-007 Add the HTTP server skeleton, middleware ordering, body/header/time limits, request IDs, RFC 7807 responses, graceful shutdown, `/livez`, and initial `/readyz`. — completed 2026-08-10, commit `72319f1`, evidence: HTTP race/coverage tests; listener/readiness/shutdown and live process smoke tests; OpenAPI lint; build/vet/source/license/vulnerability gates
- [x] ENG-008 Create a Makefile with pinned `tools`, `generate`, `fmt`, `lint`, `test`, `test-race`, `test-policy`, `test-integration`, `test-e2e`, `build`, `image`, and aggregate `verify` targets. — completed 2026-08-10, commit `7e03c72`, evidence: Makefile contract tests; individual target checks; staged clean-checkout `make verify`; intentional pre-ENG-011 image prerequisite failure
- [x] ENG-009 Add local development orchestration for pinned PostgreSQL, OPA, and optional Valkey with isolated test credentials and health checks. — completed 2026-08-10, commit `610088a`, evidence: immutable Compose image pins and contract tests; healthy full-profile runtime smoke; positive/negative PostgreSQL and Valkey authentication; OPA 1.19.0 endpoint/version check; scoped volume cleanup; `make verify`
- [x] ENG-010 Add CI for formatting, generation drift, lint, unit/race tests, policy tests, integration tests, build, vulnerability/license checks, and image build. — completed 2026-08-10, commit `47f8519`, evidence: eight least-privilege GitHub Actions jobs; immutable action pins; CI contract tests; `make verify`
- [x] ENG-011 Add a multi-stage baseline Dockerfile, `.dockerignore`, non-root runtime, build metadata, and container smoke test. — completed 2026-08-10, commit `5869296`, evidence: immutable Go/distroless base pins; allowlisted build context; UID/GID 65532; OCI labels and embedded build metadata; read-only/capability-dropped probe and SIGTERM smoke; `make verify`
- [x] ENG-012 Verify a clean checkout passes `make verify`, starts the minimal image as non-root, and commit Phase 1 evidence. — completed 2026-08-10, commit `5765834`, evidence: `docs/phase-1-evidence.md`; fresh clone of `147cbf4` remained clean after `make verify`; 6.7 MB shell-free image ran as UID/GID 65532 under hardened settings

## Phase 2 — PostgreSQL, transactions, and delivery primitives

- [x] DATA-001 Select and wrap the PostgreSQL driver, migration tool, query approach, and transaction abstraction; record the consequential decision. — completed 2026-08-19, commit `a0aacd8`, evidence: PostgreSQL adapter unit/race tests; dependency/source/license/vulnerability gates; `make verify`; `docs/adr/0001-postgresql-access-and-migrations.md`
- [x] DATA-002 Create initial migrations for tenants/principals, agents/versions/capabilities/approvals, policies/activations, and their integrity constraints/indexes. — completed 2026-08-19, commit `3901d10`, evidence: migration source/schema contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; `docs/architecture/database-schema.md`
- [x] DATA-003 Add migrations for runs, run version snapshots, state versions, signals, and append-only ordered run events. — completed 2026-08-19, commit `3901d10`, evidence: migration source/schema contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; `docs/architecture/database-schema.md`
- [x] DATA-004 Add migrations for resource dimensions, envelopes, balances, reservations, trusted usage ledger, settlements, and uniqueness/check constraints. — completed 2026-08-19, commit `3901d10`, evidence: migration source/schema contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; `docs/architecture/database-schema.md`
- [x] DATA-005 Add migrations for revocations, monotonic epochs, ordered revocation log, and gateway checkpoints. — completed 2026-08-19, commit `3901d10`, evidence: migration source/schema contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; `docs/architecture/database-schema.md`
- [x] DATA-006 Add migrations for idempotency records, audit events, and transactional outbox claim/retry/dead-letter fields. — completed 2026-08-19, commit `3901d10`, evidence: migration source/schema contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; `docs/architecture/database-schema.md`
- [x] DATA-007 Implement connection pool configuration, dependency health, transaction helpers, error classification, statement timeouts, and telemetry. — completed 2026-08-19, commit `42c3211`, evidence: configuration/adapter/telemetry unit and race tests; PostgreSQL 18.4 session-timeout and readiness integration test; `make verify`
- [x] DATA-008 Implement tenant-scoped repositories for Phase 2 tables and tests proving rollback and cross-tenant isolation. — completed 2026-08-19, commit `d58fe3b`, evidence: closed tenant-bound record repository unit/race tests; PostgreSQL 18.4 cross-tenant enumeration and transaction rollback integration test; `make verify`
- [x] DATA-009 Implement request-hash-bound idempotency acquisition, replay, conflict, expiry, and concurrent-request behavior. — completed 2026-08-19, commit `7035c44`, evidence: deterministic hash/validation/outcome unit tests; PostgreSQL 18.4 concurrent single-owner, replay, hash-conflict, TTL replacement, lease takeover, and stale-owner fencing integration/race tests; `make verify`
- [x] DATA-010 Implement transactional audit/outbox writes and a replay-safe publisher with bounded retry, jitter, leases/claims, and poison-message handling. — completed 2026-08-19, commit `3a6bb36`, evidence: atomic mutation/audit/outbox rollback and commit; PostgreSQL 18.4 concurrent claim, retry, dead-letter, and race tests; `make verify`
- [x] DATA-011 Test migrations from empty DB, upgrade from prior fixture, constraints/index access paths, forward recovery after failure, and compatibility rules. — completed 2026-08-19, commit `981c856`, evidence: immutable migration checksum manifest; PostgreSQL 18.4 empty install, seeded prior-version upgrade, constraint/index-plan, and transactional failure/recovery integration tests; `make verify`
- [x] DATA-012 Evaluate PostgreSQL row-level security as defense in depth; implement it or record why repository enforcement is used for RC. — completed 2026-08-19, commit `73588b6`, evidence: ADR-0002 threat/role/pooling evaluation; tenant repository unit/race tests; PostgreSQL 18.4 cross-tenant isolation test; `make verify`
- [x] DATA-013 Run real-PostgreSQL integration/concurrency tests and commit Phase 2 evidence. — completed 2026-08-19, commit `b79ca07`, evidence: pinned PostgreSQL 18.4 adapter race/concurrency suite; mandatory real-database `make test-integration`; `make verify`; `docs/phase-2-evidence.md`

## Phase 3 — Identity, OPA policy, and agent registry

- [x] AUTH-001 Implement OIDC issuer discovery/JWKS retrieval with pinned issuers/audiences/algorithms, cache bounds, rotation, time validation, and outage behavior. — completed 2026-08-20, commit `8f3163f`, evidence: OIDC adapter configuration/unit/race tests; same-origin discovery, bounded JWKS cache/rotation/outage tests; `make verify`
- [x] AUTH-002 Implement bearer-token middleware and verified principal/tenant/role mapping; reject conflicting body/header tenant hints. — completed 2026-08-20, commit `8f3163f`, evidence: bearer middleware/context mapping tests; configured role allowlist; forged header and tenant-body-hint rejection; `make verify`
- [x] AUTH-003 Add authentication tests for bad signature, issuer, audience, algorithm, expiry/not-before, key rotation, missing tenant, and forged forwarding headers. — completed 2026-08-20, commit `8f3163f`, evidence: adversarial OIDC and HTTP authentication race suite; `make verify`
- [x] POL-001 Implement the typed OPA client/embedded boundary with deadlines, response schema validation, reason codes, decision metadata, and fail-closed errors. — completed 2026-08-20, commit `49ca54e`, evidence: strict typed contract and bounded HTTP adapter; deadline/outage/malformed response/reason/obligation/TTL/constraint tests; `make verify`
- [x] POL-002 Implement policy bundle digest/signature verification interfaces, validation/compile testing, persistence, activation, rollback-as-new-activation, and local freshness. — completed 2026-08-20, commit `49ca54e`, evidence: SHA-256 and Ed25519 verification; compile-validator boundary; PostgreSQL 18.4 append-only activation/rollback integration; bounded monotonic local freshness; `make verify`
- [x] POL-003 Write baseline Rego for agent discovery/invocation, tenant isolation, admin actions, risk/staleness classes, constraint narrowing, and privileged operations. — completed 2026-08-20, commit `49ca54e`, evidence: pinned OPA 1.19.0 compile and baseline policy tests; `make test-policy`; `make verify`
- [x] POL-004 Add Rego unit/golden tests for allow/deny, malformed input/output, policy outage, stale state, cross-tenant access, and non-expansion of constraints. — completed 2026-08-20, commit `a9d28d2`, evidence: pinned OPA 1.19.0 17-case Rego suite; malformed/mismatched/unknown OPA output and outage race tests; `make verify`
- [x] POL-005 Add optional Valkey-backed decision caching keyed by policy version, relevant epochs, subject/action/resource/input digest, with bounded TTL and safe bypass on failure. — completed 2026-08-20, commit `a9d28d2`, evidence: normalized hashed cache-key and TTL/freshness tests; HMAC-authenticated RESP adapter; Valkey 9.1.1 auth/expiry/integrity integration; failure/poison bypass race tests; `make verify`
- [x] REG-001 Implement agent creation/update semantics, owner/sponsor/risk validation, and tenant-scoped list/describe repositories/use cases. — completed 2026-08-20, commit `356c91d`, evidence: agent aggregate/application/PostgreSQL unit and race tests; PostgreSQL 18.4 tenant isolation, principal eligibility, list/describe, and optimistic update integration; OpenAPI lint; `make verify`
- [x] REG-002 Implement immutable content-addressed agent versions with image digest, model/tool/skill/subagent declarations, limits, and schema validation. — completed 2026-08-22, commit `05e96a2`, evidence: canonical manifest/digest/schema and application unit/race tests; migration 006 checksum/schema tests; PostgreSQL 18.4 atomic registration, tenant isolation, long skill bound, duplicate digest, and direct mutation rejection integration; OpenAPI lint; `make verify`
- [x] REG-003 Implement approval/deprecation/revocation eligibility state and authorized admin endpoints with evidence records. — completed 2026-08-22, commit `ce63077`, evidence: lifecycle/application/HTTP authorization unit and race tests; migration 007 checksum/schema tests; PostgreSQL 18.4 concurrent transition, tenant isolation, atomic audit/outbox, and append-only integration tests; OpenAPI lint; `make verify`
- [x] REG-004 Implement policy-driven version resolution, optional controlled rollback/test pinning, and persisted resolution snapshots. — completed 2026-08-22, commit `1b31cbe`, evidence: resolver/domain unit and race tests; pinned OPA 1.19.0 19-case policy suite; migration 008 checksum/schema tests; PostgreSQL 18.4 eligibility, tenant isolation, stale-revocation rejection, immutable snapshot, repeatability, and migration integration tests; OpenAPI lint; `make verify`
- [x] REG-005 Implement public agent list/describe endpoints with cursor pagination, authorization filtering, and enumeration-safe errors. — completed 2026-08-22, commit `e781742`, evidence: application/HTTP authorization and cursor unit/race tests; OpenAPI lint; `make verify`
- [x] REG-006 Verify deny-by-default identity/policy behavior, immutable versions, tenant isolation, cache invalidation, and version resolution end to end. — completed 2026-08-22, commit `c91ed61`, evidence: composed HTTP identity/application/policy-cache/PostgreSQL workflow; repeated end-to-end, unit/race, policy, integration, OpenAPI/static gates; `make verify`
- [x] REG-007 Commit Phase 3 with API/policy documentation and verification evidence. — completed 2026-08-22, commit `d4af502`, evidence: `docs/phase-3-evidence.md`; API/policy documentation reconciliation; clean-tree `make verify`

## Phase 4 — Run lifecycle

- [x] RUN-001 Implement the pure run state machine with actor permissions, terminal-state rules, optimistic versioning, table-driven tests, and fuzz/property tests. — completed 2026-08-22, commit `88f8f25`, evidence: domain table/validation/immutability tests; race detector; 5-second lifecycle property fuzz campaign; `make verify`
- [x] RUN-002 Implement run admission transaction: authenticate, authorize, resolve version, narrow constraints, create initial envelope/run/event/audit/outbox records. — completed 2026-08-22, commit `a24807e`, evidence: admission domain/application/PostgreSQL unit and race tests; PostgreSQL 18.4 atomic commit, eligibility, evidence, and forced-final-insert rollback integration test; `make verify`
- [x] RUN-003 Implement `POST /v1/agents/{agent_id}/runs` with request limits, idempotency, server-derived tenant, explicit-version restrictions, and stable errors. — completed 2026-08-22, commit `b08be0f`, evidence: authenticated HTTP boundary and replay tests; PostgreSQL 18.4 idempotency/admission integration; focused unit/race tests; OpenAPI lint; `make verify`
- [x] RUN-004 Implement tenant-scoped `GET /v1/runs/{run_id}` with authorization and enumeration-safe not-found behavior. — completed 2026-08-22, commit `636e390`, evidence: tenant-bound PostgreSQL projection and cross-tenant integration test; application policy allow/deny tests; authenticated HTTP and malformed-ID tests; focused unit/race/OpenAPI/full verification
- [x] RUN-005 Implement validated, typed, idempotent `POST /v1/runs/{run_id}/signals` with ordered events and delivery semantics. — completed 2026-08-22, commit `6f3fa4b`, evidence: domain/application/HTTP unit and race tests; PostgreSQL 18.4 atomic evidence, replay, tenant/version, and concurrent ordering integration tests; OpenAPI lint; `make verify`
- [x] RUN-006 Implement idempotent authorized `POST /v1/runs/{run_id}/cancel` including races with terminal transitions. — completed 2026-08-22, commit `1ef9ac1`, evidence: domain/application/HTTP unit and race tests; PostgreSQL 18.4 atomic evidence, tenant/version, replay, and repeated terminal-race integration tests; OpenAPI lint; `make verify`
- [x] RUN-007 Implement ordered/resumable `GET /v1/runs/{run_id}/events` using SSE, cursor validation, heartbeats, backpressure, retention, and authorization. — completed 2026-08-22, commit `45d8c14`, evidence: authenticated cursor/application/HTTP unit and race tests; PostgreSQL 18 ordered resume and tenant-isolation integration; OpenAPI lint; `make verify`
- [x] RUN-008 Implement worker-facing claim/heartbeat/start/complete/fail/timeout operations with leases and fencing so stale workers cannot mutate runs. — completed 2026-08-22, commit `289dd2f`, evidence: lease/domain/application unit and race tests; PostgreSQL 18.4 expiry/reclaim, concurrent claim, monotonic fencing, ordered-event, lifecycle, and stale-worker integration tests; `make verify`
- [x] RUN-009 Add outbox publication for every run mutation and verify at-least-once/replay behavior after worker/process crashes. — completed 2026-08-22, commit `d0d88df`, evidence: transactional worker event/outbox identity tests; PostgreSQL 18.4 post-send crash and lease-expiry replay integration repeated 10 times; focused race suite; `make verify`
- [x] RUN-010 Run OpenAPI conformance, lifecycle concurrency/idempotency, tenant isolation, SSE reconnect, and complete end-to-end tests. — completed 2026-08-22, commit `81b7508`, evidence: mounted HTTP/application/PostgreSQL lifecycle workflow; OpenAPI response-shape and lint checks; exact admission/signal/cancel replay and hash conflict; cross-tenant not-found; SSE `Last-Event-ID` reconnect; terminal race repeated 10 times and race detector; complete end-to-end suite; `make verify`
- [x] RUN-011 Commit Phase 4 with API examples, state-machine documentation, and evidence. — completed 2026-08-22, commit `6518df7`, evidence: `docs/api/run-lifecycle.md`, `docs/contracts/domain-model.md`, `docs/phase-4-evidence.md`; OpenAPI lint; clean-tree `make verify`

## Phase 5 — Resource allocation and run governance

- [x] RES-001 Implement extensible typed resource dimensions classified as consumable, structural, or deadline-like with explicit units and validation. — completed 2026-08-25, commit `a1ada23`, evidence: domain definition/value validation unit and race tests; migration 009 checksum/schema tests; PostgreSQL 18.4 forward-upgrade and constraint qualification; `make verify`
- [x] RES-002 Implement policy-constrained root envelope issuance and persist its immutable grant plus mutable balances. — completed 2026-08-25, commit `43d04ee`, evidence: exact policy grant-vector domain unit/race tests; PostgreSQL 18.4 atomic issuance, initial conservation, bounds rollback, and grant immutability integration tests; migration 010 upgrade qualification; end-to-end lifecycle; `make verify`
- [x] RES-003 Implement multi-dimension atomic child reservation using PostgreSQL locks/conditional updates and deterministic lock ordering. — completed 2026-08-26, commit `8a37a87`, evidence: typed vector validation and deterministic-order unit/race tests; PostgreSQL 18.4 multi-resource conservation, insufficient-vector rollback, and inverse-order concurrent oversubscription test repeated 5 times; `make verify`
- [x] RES-004 Enforce maximum active/total children and delegation depth transactionally during child admission. — completed 2026-08-26, commit `996d48b`, evidence: atomic authorized-child admission composition; parent-envelope serialization; active/lifetime/depth and non-expansion checks; PostgreSQL 18.4 rollback and concurrent-ceiling integration repeated 3 times; `make verify`
- [x] RES-005 Implement trusted metering ingestion with authenticated producers, unique source event IDs, monotonic application, and invalid/negative usage rejection. — completed 2026-08-26, commit `de85483`, evidence: authenticated/policy-bound HTTP and application tests; deterministic event validation/digest tests; PostgreSQL 18.4 replay, mismatched duplicate, conservation, and over-budget integration; full integration suite; `make verify`
- [x] RES-006 Implement rate enforcement for structural throughput limits; use Valkey only as a conservative accelerator with defined outage behavior. — completed 2026-08-26, commit `e4adb0f`, evidence: domain/application/Valkey unit and race tests; PostgreSQL 18.4 atomic fixed-window enforcement and 10-way concurrency repeated 3 times; Valkey 9.1.1 marker expiry/outage bypass; `make verify`
- [x] RES-007 Implement consumable exhaustion detection and the `BUDGET_EXHAUSTED`, `PAUSED_FOR_BUDGET`, and `FAILED_BUDGET` transitions. — completed 2026-08-26, commit `534fdbe`, evidence: domain/application/policy unit and race tests; PostgreSQL 18.4 atomic zero-balance lifecycle/evidence and concurrent exact-exhaustion qualification repeated 3 times; pinned OPA 1.19.0 20-case suite; `make verify`
- [x] RES-008 Implement authorized deadline/budget extension as an additive ledger action with policy approval and privileged evidence; prevent self-extension. — completed 2026-08-26, commit `461281e`, evidence: domain/application/HTTP/policy unit and race tests; migration 013 checksum/schema and PostgreSQL 18.4 forward-upgrade qualification; atomic additive grant/deadline/resume/evidence integration; replay/mismatch checks; pinned OPA 1.19.0 21-case suite; `make verify`
- [x] RES-009 Implement idempotent terminal settlement that atomically closes a reservation and immediately returns unused capacity to the parent. — completed 2026-08-26, commit `e95c5f1`, evidence: domain/application/authenticated HTTP unit and race tests; migration 014 checksum/schema and PostgreSQL 18.4 forward-upgrade qualification; authoritative multi-dimension consumption, atomic parent credit/close/evidence, concurrent exact replay, and open-descendant rejection; `make verify`
- [x] RES-010 Implement reconciliation for terminal/expired orphan reservations and replay after crashes without double credit. — completed 2026-08-26, commit `44a247b`, evidence: bounded application worker and tenant-scoped `SYSTEM` authority tests; PostgreSQL 18.4 terminal/expiry selection, atomic timeout/fencing/reclaim/evidence, forced rollback, concurrent claim, and crash replay repeated 3 times; `make verify`
- [x] RES-011 Add invariant/property tests over random reservation/usage/settlement sequences and checked arithmetic bounds. — completed 2026-08-26, commit `1d9adfc`, evidence: randomized allocation conservation and checked decimal arithmetic fuzz campaigns; PostgreSQL 18.4 settlement conservation integration; domain/PostgreSQL unit and race suites; `make verify`
- [x] RES-012 Add high-concurrency tests proving no oversubscription, deadlock, lost credit, double settlement, or structural-limit bypass. — completed 2026-08-26, commit `014954a`, evidence: 32-way synchronized inverse-order multi-dimension reservation, terminal settlement replay, and governed child-admission contention against PostgreSQL 18.4 repeated 5 times; exact conservation, durable-row, and single-settlement assertions; focused race suite; `make verify`
- [x] RES-013 Add end-to-end parent/child allocation, exhaustion, extension, settlement, reclaim, and sibling reuse tests. — completed 2026-08-26, commit `edf1dec`, evidence: composed application/PostgreSQL allocation lifecycle; child exhaustion/pause, authorized additive extension/resume, terminal settlement, expiry timeout/reclaim, and exact sibling capacity reuse; focused race suite; `make verify`
- [x] RES-014 Commit Phase 5 with resource model documentation, database evidence, and load/concurrency results. — completed 2026-08-26, commit `775539f`, evidence: `docs/contracts/resource-accounting.md`; `docs/architecture/database-schema.md`; `docs/phase-5-evidence.md`; 32-way PostgreSQL contention and randomized conservation qualification; composed resource lifecycle; clean-tree `make verify`

## Phase 6 — Revocation distribution and freshness

- [x] REV-001 Implement typed revocation creation/lift for every supported scope with authorization, reason, effective/expiry time, and evidence. — completed 2026-08-27, commit `d77dec1`, evidence: domain/application/authenticated HTTP tests; authoritative OPA policy; PostgreSQL 18.4 atomic change/audit/outbox integration
- [x] REV-002 Implement atomic monotonic epoch increment plus ordered log/outbox append; prove epochs never decrement or duplicate under concurrency. — completed 2026-08-27, commit `d77dec1`, evidence: PostgreSQL 18.4 16-way global/tenant epoch concurrency repeated 3 times; exact monotonic epoch and unique sequence assertions; atomic lift evidence
- [x] REV-003 Enforce revocations and applicable epoch checks in discovery, admission, lifecycle mutations, worker operations, and policy evaluation. — completed 2026-08-27, commit `72ff9de`, evidence: authoritative policy-evaluator boundary for every scope and exact applicable epochs; governed worker claim/heartbeat/mutation checks plus PostgreSQL claim exclusion; unit/race, PostgreSQL integration, and clean-tree `make verify`
- [x] REV-004 Implement resumable authenticated revocation SSE stream with sequence/epoch cursor, heartbeat, backpressure, retention, and gap signaling. — completed 2026-08-27, commit `72ff9de`, evidence: HMAC tenant-bound sequence/security-epoch cursor; authenticated reconnect; 256-entry polling, per-write deadlines, heartbeats, logical retention, pre-stream 410 and terminal gap signaling; HTTP/domain tests and `make verify`
- [x] REV-005 Implement authoritative reconciliation/snapshot endpoint and persisted gateway checkpoints. — completed 2026-08-27, commit `72ff9de`, evidence: authenticated delta-or-content-addressed-snapshot reconciliation; 10,000-change bound; effective/lifted/expired active-state reconstruction; monotonic checkpoint upsert; PostgreSQL 18.4 integration and `make verify`
- [x] REV-006 Implement instance-local freshness tracker with last stream receipt, last reconciliation, last applied epoch, and clock-safe age calculation. — completed 2026-08-27, commit `8409b99`, evidence: tenant-scoped concurrent tracker; sparse epoch merge and sequence/epoch regression rejection; wall-clock rollback and monotonic-clock fail-closed tests; unit/race suites and `make verify`
- [x] REV-007 Enforce risk-based freshness: authoritative high-risk writes, default 30-second normal writes, 60-second sensitive reads, and bounded low-risk cache. — completed 2026-08-27, commit `2398385`, evidence: risk/action classification and tracker-derived security-state enforcement; live zero-TTL high-risk bypass; operation-bound cache keys/TTL; 24-case OPA suite; unit/race and `make verify`
- [x] REV-008 Wire revocation/policy activation changes to local and Valkey cache invalidation/versioned cache keys. — completed 2026-08-27, commit `c7856ff`, evidence: bounded tenant-local decision cache; global/tenant generation-bound Valkey keys; direct revocation create/lift, stream/reconciliation, and policy activation/rollback invalidation; unit/race, PostgreSQL integration, and clean-tree `make verify`
- [x] REV-009 Expose revocation age/lag/gap metrics and make readiness reflect inability to meet configured security freshness. — completed 2026-08-27, commit `90f913a`, evidence: bounded-cardinality scrape-time age/lag/gap/incident/tenant/health metrics; unknown/stale/lag/gap/clock-regression readiness tests; HTTP readiness composition; unit/race and clean-tree `make verify`
- [x] REV-010 Test stream gaps, duplicate/out-of-order events, restarts, partitions, expired cache, reconciliation, global revocation, and fail-closed recovery. — completed 2026-08-27, commit `bf8eae1`, evidence: composed tracker/freshness/cache/authoritative-policy resilience tests; focused unit/race suites; clean-tree `make verify`
- [x] REV-011 Measure propagation against SLO targets and commit Phase 6 with documented gateway integration behavior. — completed 2026-08-27, commit `57888c2`, evidence: 100-change authoritative PostgreSQL-to-authenticated-SSE measurement p50 10.239 ms, p99 22.463 ms, max 33.979 ms against p99 <= 5 s; three-run stability qualification; `docs/phase-6-evidence.md`; clean-tree `make verify`

## Phase 7 — Governance self-protection and independent evidence

- [x] SEC-001 Classify endpoints/actions by risk and enforce separate least-privilege service/admin roles through policy. — completed 2026-08-31, evidence: closed fail-conservative action-risk catalog shared with revocation freshness; non-hierarchical caller/workload/meter/settler/gateway and registry/resource/policy/revocation role policy; gateway distribution policy authorization; contract/security documentation; 24-case OPA suite, unit tests, and e2e-tag compile
- [x] SEC-002 Implement approval-provider interfaces and four-eyes state for trust-root rotation, global revocation changes, policy bypass/rollback, emergency expansion, and privileged agent classes. — completed 2026-08-31, evidence: closed protected-action catalog; replaceable challenge/assertion provider port; independent digest-bound, expiring, single-use state machine; append-only PostgreSQL requests/decisions/consumptions; domain/application/schema tests and contract/security documentation
- [x] SEC-003 Implement signer/verifier abstractions compatible with managed KMS/HSM and ensure production private keys are non-exportable/not file-configured. — completed 2026-08-31, evidence: provider-neutral SHA-256 digest signer/verifier/key-inspector ports; managed key metadata and signature binding guards; production KMS/HSM-only configuration with no private-key/file fields; substitution, exportability, purpose, outage, and configuration tests; contract/security documentation
- [x] SEC-004 Require signed/versioned policy and privileged configuration artifacts and reject digest/signature/version mismatch. — completed 2026-09-01, evidence: closed policy/trust-root/revocation/resource-override/privileged-agent artifact catalog; domain-separated deterministic signed envelope using SEC-003 managed-verifier metadata; payload/digest/kind/schema/revision/key-version/algorithm/signature substitution tests; versioned OpenAPI and contract documentation; migration 016 legacy-validation rejection, unique revisions, PostgreSQL 18.4 activation/rollback integration; unit/race/vet and OpenAPI gates
- [x] SEC-005 Finalize evidence event schemas for policy, revocation, resource overrides, approvals, key lifecycle, version approvals, and break-glass actions. — completed 2026-09-01, evidence: closed `thinkpixelag.evidence/v1` envelope and seven-type catalog; machine-readable JSON Schema and provider-neutral Go validation boundary; strict unknown-field/type-substitution/digest/bounds/time/epoch tests; contract, compatibility, minimization, and SEC-006 transport-boundary documentation; unit/race/vet and repository verification gates
- [x] SEC-006 Implement authenticated export to a separately configurable evidence sink with replay, receipt/checkpoint, and tamper-evident hash/link metadata. — completed 2026-09-01, evidence: closed `thinkpixelag.evidence-delivery/v1` contract; HTTPS bearer-authenticated bounded sink adapter; stable event-ID idempotency; deterministic per-sink SHA-256 chain; leased replay and stale-claim fencing; append-only receipt and monotonic checkpoint migration; receipt-substitution, tampering, replay, configuration-redaction, unit/race, and real-PostgreSQL integration tests
- [x] SEC-007 Define and implement a narrow, time-bound, strongly authenticated break-glass workflow with explicit expiry and separate evidence. — completed 2026-09-01, evidence: closed `thinkpixelag.break-glass/v1` policy/revocation recovery scopes; replaceable phishing-resistant MFA verifier with five-minute freshness and identity binding; exact grant-digest four-eyes consumption; random one-time credential with digest-only storage; database-enforced fifteen-minute maximum, append-only activation/use/expiry/revocation, atomic v1 evidence outbox; substitution, replay, stale-authentication, expiry, unit/race, and real-PostgreSQL tests
- [x] SEC-008 Add workload-identity/mTLS integration hooks and explicit service-to-service authorization; document local-development exceptions.
- [x] SEC-009 Complete secret/token/payload redaction tests across logs, traces, metrics, errors, policy records, audit, and panic paths.
- [x] SEC-010 Run security tests for privilege escalation, approval bypass, replay, cache poisoning, policy tampering, stale authorization, and evidence suppression. — completed 2026-09-01, evidence: stable `make test-security` gate covering adversarial Go and 26/26 Rego cases plus real-PostgreSQL approval immutability, replay fencing, transactional audit/outbox atomicity, delivery receipts, and monotonic checkpoints; full `make verify` passed; implementation commit `8fc7875`
- [x] SEC-011 Review the threat model against implementation, resolve all critical/high findings, and commit Phase 7 evidence. — completed 2026-09-01, evidence: all T01–T22 controls traced to executable proofs; zero open critical/high findings; medium Phase 8 residual risks explicitly registered; `make test-security` passed 26/26 Rego, selected Go, and real-PostgreSQL security cases; `make verify` passed; `docs/phase-7-evidence.md`; implementation commit `c5f8ddc`

## Phase 8 — Kubernetes, operations, and release engineering

- [x] OPS-001 Finalize reproducible multi-architecture OCI build with pinned base digests, non-root user, read-only filesystem compatibility, CA roots, labels, and graceful shutdown. — completed 2026-09-02, evidence: digest-pinned Buildx `linux/amd64,linux/arm64` OCI manifest `sha256:b6fc5c9712f1b27ba2046a1fa8bcbdedf5bbb307d782a50035203b42491d7d16`; shell-free UID 65532, CA-root/read-only/SIGTERM smoke; implementation commit `967165a`
- [x] OPS-002 Generate SBOM, vulnerability report, checksum, provenance, and image-signing/verification hooks in CI with release thresholds. — completed 2026-09-02, evidence: CycloneDX, Trivy JSON and zero critical/high threshold, deterministic checksums, SLSA-shaped provenance, BuildKit attestations, cosign hooks; implementation commit `967165a`
- [x] OPS-003 Create Kubernetes Deployment, Service, ServiceAccount, ConfigMap, secret references, and pre-deploy migration Job. — completed 2026-09-02, evidence: Kustomize render and disposable-cluster migration-before-readiness passed; implementation commit `967165a`
- [x] OPS-004 Add restricted security context, seccomp, dropped capabilities, NetworkPolicies, resource requests/limits, topology spreading, and PodDisruptionBudget. — completed 2026-09-02, evidence: contract/render gates plus kind restricted runtime and PDB-blocked drain; implementation commit `967165a`
- [x] OPS-005 Add readiness/liveness/startup probes that reflect process health, DB/policy readiness, and revocation freshness appropriately. — completed 2026-09-02, evidence: process-only startup/liveness; production `/readyz` composition for PostgreSQL, at least one currently valid loaded active policy, and 30-second authoritative revocation reconciliation; stale/empty/failure recovery tests; real PostgreSQL integration; clean-tree `make verify`; implementation commits `e528009`, `01135d6`
- [x] OPS-006 Add optional HPA and ServiceMonitor/PodMonitor plus dashboards for API, policy, database, outbox, allocation, run, revocation, cache, and Go runtime signals. — completed 2026-09-02, evidence: optional autoscaling/scrape resources; dashboard coverage for every required signal; live HTTP/database/revocation/Go collectors; 15-second PostgreSQL outbox/settlement snapshot publisher; scrape-time pool saturation; zero-initialized bounded outcome vocabularies; focused unit/race and real PostgreSQL integration; clean-tree `make verify`; implementation commit `41ab200`
- [x] OPS-007 Define actionable alerts tied to SLOs with severity, ownership, runbook links, and anti-noise windows. — completed 2026-09-02, evidence: 27-rule PrometheusRule matrix for public-read/run-mutation/admin-trusted availability budgets, p95/p99 API and OPA latency, database/policy/cache health, revocation reconciliation, contention, outbox, and settlement; every rule has a sustained `for` window, severity, owner, summary, and linked runbook; native Prometheus 3.5 rule validation, focused unit/race checks, and clean-tree `make verify`; implementation commit `bd203e5` (supersedes the incomplete foundation evidence at `967165a`)
- [x] OPS-008 Write runbooks for install/configure, migration, upgrade/rollback, backup/restore, policy rollback, revocation gaps, DB/OPA/Valkey outage, outbox backlog, key rotation, and break-glass. — completed 2026-09-02, evidence: `docs/operations/runbooks.md`, recovery and load qualification procedures; implementation commit `967165a`
- [x] OPS-009 Test PostgreSQL backup/restore, point-in-time recovery assumptions, schema forward recovery, and restoration of epoch/outbox/allocation invariants. — completed 2026-09-02, evidence: isolated PostgreSQL 18.4 continuous WAL archive; AES-256 encrypted physical base backup; named recovery immediately before and after an atomic governance transaction; exact global/tenant/agent epoch, audit/outbox, and allocation assertions; forward migration from schema 17 to 18; restored invariant checker; observed local RTO 3–4 seconds and zero selected-target transaction loss; clean-tree `make verify`; implementation commit `fa04a0c`
- [ ] OPS-010 Run load tests for target API/policy latency, run admission throughput, allocation contention, SSE fanout, and outbox/revocation lag; tune documented limits. — deterministic policy and streaming baselines implemented 2026-09-03 in `0710e5c`; completion remains blocked until the production process mounts the governed routes and the full suite runs against the intended topology with percentile, saturation, invariant, and tuned-limit evidence
- [ ] OPS-011 Run resilience tests for DB failover/latency, OPA outage/malformed response, Valkey loss, stream partitions, worker crash, pod eviction, and rolling restart. — repository-local matrix implemented 2026-09-03 in `4fff867`: OPA timeout/malformed output, conservative cache loss/poison recovery, revocation partition/reconciliation, expired worker fencing, evidence retry, PostgreSQL latency/process crash, API pod crash, and rolling restart passed; completion still requires target-platform PostgreSQL replica promotion plus production-composed Valkey and worker crash evidence
- [ ] OPS-012 Run disposable-cluster install, restricted-container smoke, migration, core workflow, HPA/disruption, upgrade, rollback, and uninstall tests. — strengthened disposable kind coverage passed 2026-09-03 in `4fff867`: install, schema 18 migration, exact restricted-container posture, readiness/metrics, HPA bounds, PDB-blocked drain, pod replacement, rolling configuration rollback, and complete uninstall; completion still requires a governed core API workflow through the production composition, observed HPA scale behavior, and upgrade/rollback between two immutable application digests
- [x] OPS-013 Add release automation that produces immutable image digests, manifests/chart package, SBOM/provenance, API artifacts, checksums, and draft notes. — completed 2026-09-02, evidence: tag-gated `.github/workflows/release.yaml`, deterministic Kubernetes/API archive, image manifest/metadata, SBOM/provenance, checksums, GitHub attestations, draft notes; implementation commit `967165a`
- [ ] OPS-014 Commit Phase 8 with operational docs, test reports, and artifact-generation evidence.

## Phase 9 — Release-candidate closure

- [ ] RC-001 Freeze the OpenAPI and policy decision contracts; run backward-compatibility and generated-artifact drift checks.
- [ ] RC-002 Run `make verify` from a clean checkout and archive unit, race, fuzz, policy, integration, contract, end-to-end, security, and Kubernetes smoke evidence.
- [ ] RC-003 Confirm SLO/capacity targets and document accepted performance envelopes and scaling guidance.
- [ ] RC-004 Confirm zero unresolved critical/high vulnerabilities, security findings, migration risks, flaky/skipped required tests, or fail-open paths.
- [ ] RC-005 Exercise and record install, upgrade, rollback/forward, backup/restore, policy rollback, revocation reconciliation, key rotation, and break-glass game days.
- [ ] RC-006 Reconcile every TODO item against implementation, tests, commits, docs, and release artifacts; resolve all blockers and deviations.
- [ ] RC-007 Update README with final architecture, supported versions, quick start, configuration, API docs, development commands, deployment, security, and support status.
- [ ] RC-008 Create numbered ADRs under `docs/adr/` for all durable decisions and relevant history extracted from `PLAN.md` and `TODO.md`.
- [ ] RC-009 Verify ADRs preserve all useful rationale, alternatives, consequences, risks, and implementation/commit references from the living plan.
- [ ] RC-010 Prepare RC release notes, known limitations, operator checklist, artifact inventory, upgrade notes, and proposed semantic version/tag.
- [ ] RC-011 Remove `PLAN.md` and `TODO.md`, validate links/docs and run `make verify` in the resulting tree.
- [ ] RC-012 Commit the final ADR/documentation transition to `main`, build the release artifacts from that exact commit, and verify digest/provenance consistency.

## Progress log

Append one entry per completed atomic item or tightly coupled group. Do not delete historical entries; supersede them with a later note.

| Date | TODO IDs | Commit | Verification evidence | Notes/deviations |
|---|---|---|---|---|
| 2026-08-10 | GOV-001–GOV-013 | `eddf453` | Redocly CLI 2.3.0 lint; `git diff --check`; blueprint coverage review | Exact dependency versions deferred to Phase 1 testing by design |
| 2026-08-10 | ENG-001 | `5287791` | `go version`; `go mod edit -json`; `go test ./...`; `git diff --check` | Go 1.26.5 tested; command behavior remains scoped to later Phase 1 items |
| 2026-08-10 | ENG-002 | `45c422d` | source-policy audit; `govulncheck -test ./...`; `go-licenses check --include_tests`; root/tools tests; `git diff --check` | No dependency exceptions; Make and CI wiring remain ENG-008/ENG-010 |
| 2026-08-10 | ENG-003 | `be384bc` | configuration/redaction tests; `go test -race ./...`; `go vet ./...`; source/license/vulnerability gates | Secret values are environment-only; command startup wiring remains ENG-007 |
| 2026-08-10 | ENG-004 | `f6c3a9e` | structured logging, correlation spoofing, recursive redaction and handler immutability tests; race/vet/source/license/vulnerability gates | Trace population and HTTP request-ID middleware remain ENG-005/ENG-007 |
| 2026-08-10 | ENG-005 | `215bfbf` | metrics and OTLP integration tests; `go test -race -cover ./...`; vet/source/license/vulnerability gates | Eight exact upstream pseudo-version exceptions expire 2027-02-10; metrics endpoint/server wiring remains ENG-007 |
| 2026-08-10 | ENG-006 | `8ff8f77` | UUIDv7, UTC, exact decimal/quantity, authenticated cursor, and typed-error tests; ID/decimal/cursor fuzz campaigns; race/vet/source/license/vulnerability gates | Primitives add no dependencies; HTTP RFC 7807 mapping and cursor-key configuration remain ENG-007 |
| 2026-08-10 | ENG-007 | `72319f1` | request/trace correlation, recovery/redaction, limits, RFC 7807, metrics, probe, readiness lifecycle, listener shutdown, OpenAPI, and process smoke evidence; full race/vet/build/source/license/vulnerability gates | Initial readiness is process-local; PostgreSQL, policy, and revocation freshness gates remain in their owning phases |
| 2026-08-10 | ENG-008 | `7e03c72` | Makefile contract tests; exact-version tool, generation/format/lint, unit/race/policy/integration/e2e, security, build, and aggregate verification evidence | `image` intentionally requires ENG-011's Dockerfile and remains outside `verify` until then; OPA/dependency-backed suites report their scheduled absence explicitly |
| 2026-08-10 | ENG-009 | `610088a` | immutable Compose pin/health/isolation contract tests; full PostgreSQL 18.4, OPA 1.19.0, and Valkey 9.1.1 runtime smoke; positive/negative authentication; `make verify` | Host ports are loopback-only; Valkey is opt-in and non-persistent; PostgreSQL state survives `dev-down` and only scoped `dev-reset` removes it; OPA starts policy-empty until Phase 3 |
| 2026-08-10 | ENG-010 | `47f8519` | CI contract tests; formatting/generation/lint, unit/race/policy/integration, dependency/vulnerability/license, binary and image jobs; `make verify` | Third-party actions use full commit SHAs; ENG-011 subsequently activated the image job as a mandatory build and runtime-smoke gate |
| 2026-08-10 | ENG-011 | `5869296` | Docker contract tests; pinned multi-stage build; OCI metadata; non-root/read-only/probe/SIGTERM runtime smoke; `make verify` | Distroless runtime intentionally has no shell; Go builder and runtime indexes support both required `linux/amd64` and target `linux/arm64`; multi-architecture publishing remains OPS-001 |
| 2026-08-10 | ENG-012 | `5765834` | fresh isolated clone at `147cbf4`; clean before/after `make verify`; image ID/size/metadata; shell-free UID 65532 read-only/probe/SIGTERM smoke | Phase 1 qualified on `linux/amd64`; hosted CI awaits push; `linux/arm64` qualification remains OPS-001 |
| 2026-08-19 | DATA-001 | `a0aacd8` | pgx/Tern adapter unit and race tests; dependency/source/license/vulnerability gates; `make verify`; hardened container smoke | Selected pgx v5.10.0, Tern v2.4.1, handwritten SQL, and callback-owned transactions; advanced Go/build pin to 1.26.6 after the gate found fixed standard-library vulnerabilities |
| 2026-08-19 | DATA-002–DATA-006 | `3901d10` | contiguous migration and schema-contract tests; PostgreSQL 18.4 apply/reverse rehearsal; `make verify`; hardened container smoke | Composite tenant foreign keys prevent cross-tenant relationships; global ordered streams are indexed separately; repository/role immutability enforcement remains DATA-007/DATA-008 as documented |
| 2026-08-19 | DATA-007 | `42c3211` | configuration, pool, error classification, bounded telemetry, and race tests; PostgreSQL 18.4 session-timeout/readiness integration; `make verify` | Readiness fails closed on database health while liveness remains independent; telemetry excludes SQL text and parameters; repository isolation remains DATA-008 |
| 2026-08-19 | DATA-008 | `d58fe3b` | closed tenant-bound record kinds; tenant-first parameterized identity queries; enumeration-safe not-found behavior; transaction rebinding; unit/race tests; PostgreSQL 18.4 isolation and rollback integration; `make verify` | Composite-key child rows remain reachable only through their parent aggregate; typed aggregate mutations arrive in their owning phases; RLS remains DATA-012 |
| 2026-08-19 | DATA-009 | `7035c44` | deterministic request hashing and validation; PostgreSQL 18.4 single-owner concurrency, response replay, request-hash conflict, TTL expiry, lease takeover, and stale-owner fencing; unit/integration/race tests; `make verify` | Request normalization remains transport-specific; failed or abandoned same-hash work receives a new fenced owner token, while completed results replay until TTL expiry |
| 2026-08-19 | DATA-010 | `3a6bb36` | deterministic audit hashing and validation; atomic mutation/audit/outbox rollback and commit; PostgreSQL 18.4 competing `SKIP LOCKED` claims, fenced publication, bounded jittered retry, permanent poison dead-lettering, unit/integration/race tests; `make verify` | Delivery is at least once; sinks deduplicate by stable message UUID. Dead letters remain durable for operator inspection/replay |
| 2026-08-19 | DATA-011 | `981c856` | immutable migration checksum manifest; PostgreSQL 18.4 empty install, seeded prior-version upgrade, check/unique/foreign-key constraints, tenant/outbox index plans, transactional failure rollback and forward recovery; `make verify` | Compatibility follows expand/migrate/contract; released migrations are checksum-locked and corrections use new forward migrations |
| 2026-08-19 | DATA-012 | `73588b6` | ADR-0002 threat/role/pooling evaluation; tenant repository unit/race tests; PostgreSQL 18.4 cross-tenant isolation; `make verify` | RC retains explicit repository enforcement because current pooled non-transactional access, owner-role bypass, and global workers make partial RLS ineffective; reevaluation criteria require separated roles and transaction-bound tenant context |
| 2026-08-19 | DATA-013 | `b79ca07` | PostgreSQL 18.4 adapter race/concurrency suite; mandatory real-database integration target and CI service; full `make verify`; `docs/phase-2-evidence.md` | Phase 2 exit gate passed with no required PostgreSQL skips; integration now fails when its real database is unavailable |
| 2026-08-20 | AUTH-001–AUTH-003 | `8f3163f` | OIDC discovery/JWKS, JWT validation, rotation/outage, claim mapping, bearer middleware, and adversarial race tests; `make verify` | One pinned issuer/audience per process; same-origin JWKS; RS256 default with optional ES256; known keys survive outages only within the configured stale bound; forwarded/body tenant hints fail closed |
| 2026-08-20 | POL-001–POL-003 | `49ca54e` | typed OPA boundary and fail-closed race tests; signed-bundle and freshness tests; PostgreSQL 18.4 activation/rollback integration; pinned OPA 1.19.0 policy tests; `make verify` | Policy rollback appends a higher activation version; process-local activation age is bounded; managed KMS/HSM verification remains SEC-003 and decision caching remains POL-005 |
| 2026-08-20 | POL-004–POL-005 | `a9d28d2` | pinned OPA 1.19.0 17-case golden suite; OPA adversarial/outage race tests; cache key/TTL/bypass/poison tests; Valkey 9.1.1 auth/expiry/HMAC integration; `make verify` | Valkey is optional and never authoritative; cache entries require a separately managed HMAC secret, and rotation invalidates old entries safely |
| 2026-08-20 | REG-001 | `356c91d` | agent aggregate/application/PostgreSQL unit and race tests; PostgreSQL 18.4 tenant isolation, principal eligibility, list/describe, and optimistic update integration; OpenAPI lint; `make verify` | Creation requires distinct active same-tenant owner/sponsor principals; retired agents cannot be restored; updates use `updated_at` compare-and-swap; HTTP authorization/filtering remains REG-005 |
| 2026-08-22 | REG-002 | `05e96a2` | canonical manifest/digest/schema and application unit/race tests; migration 006 checksum/schema tests; PostgreSQL 18.4 atomic registration, isolation, duplicate/failure handling, and direct update/delete rejection; OpenAPI lint; `make verify` | Schema-v1 hashes compact field-ordered JSON after sorting declarations; versions and capabilities commit in one statement; database triggers enforce artifact immutability; approval state remains REG-003 |
| 2026-08-22 | REG-003 | `ce63077` | lifecycle/application/HTTP authorization unit and race tests; migration 007 checksum/schema tests; PostgreSQL 18.4 concurrent transition, tenant isolation, atomic audit/outbox, and append-only integration tests; OpenAPI lint; `make verify` | Decisions require an authoritative `versions.approve` policy ALLOW; version-row locking serializes competing transitions; decision, audit, and outbox evidence commit atomically |
| 2026-08-22 | REG-004 | `1b31cbe` | resolver/domain unit and race tests; pinned OPA 1.19.0 19-case policy suite; migration 008 checksum/schema tests; PostgreSQL 18.4 eligibility, tenant isolation, stale-revocation rejection, immutable snapshot, repeatability, and migration integration tests; OpenAPI lint; `make verify` | Automatic selection tries approved versions newest-first; explicit approved pins and deprecated rollback require separate zero-TTL governance decisions under the same active policy version as invocation |
| 2026-08-22 | REG-005 | `e781742` | authenticated public list/describe handler tests; policy filtering, invocability, pagination, tenant-boundary, outage, malformed/tampered cursor, and enumeration-safe error unit/race tests; OpenAPI lint; `make verify` | Discovery authorizes the catalog and each candidate independently; only active agents with an approved version are exposed, and every continuation re-evaluates current policy and eligibility |
| 2026-08-22 | REG-006 | `c91ed61` | composed HTTP identity, application, policy-cache, and real PostgreSQL workflow; repeated end-to-end test; unit/race, policy, integration, OpenAPI/static gates; `make verify` | End-to-end tests now require real PostgreSQL; policy activation and relevant epoch changes invalidate authorization cache keys; test-created outbox evidence is scoped and cleaned to preserve suite isolation |
| 2026-08-22 | REG-007 | `d4af502` | Phase 3 API/policy/evidence reconciliation; approval concurrency test repeated 20 times against PostgreSQL 18.4; clean-tree `make verify` | Exit verification found and corrected a `READ COMMITTED` snapshot race by separating version locking from current-state evaluation within one transaction; run admission and remaining lifecycle transport assembly begin in Phase 4 |
| 2026-08-22 | RUN-001 | `88f8f25` | Pure domain transition table, validation, immutability, terminal replay, unit/race tests, and 5-second property fuzz campaign; `make verify` | Authorized same-terminal replays return the established state despite a stale expected version; every actual change requires compare-and-swap and increments the state version exactly once |
| 2026-08-22 | RUN-002 | `a24807e` | Authenticated admission orchestration, fail-closed policy/version resolution, narrowed-constraint and deadline tests; PostgreSQL 18.4 all-record commit and forced-final-insert rollback; unit/race/integration suites; `make verify` | Root `PENDING` is transaction-local: only the complete `ADMITTED` version-1 aggregate becomes durable. Restricted objective/input never enter governance evidence; dimension grants/balances remain Phase 5 resource work |
| 2026-08-22 | RUN-003 | `b08be0f` | Strict authenticated admission HTTP tests; scoped normalized idempotency replay/conflict persistence against PostgreSQL 18.4; unit/race/OpenAPI/full verification | Objective/input participate only in the request hash and never enter governance or evidence. The Phase 4 envelope response carries its version and optional deadline; resource quantities remain Phase 5 work |
| 2026-08-22 | RUN-004 | `636e390` | Tenant-bound PostgreSQL projection and cross-tenant integration test; application authorization and authenticated HTTP enumeration tests; focused unit/race/OpenAPI/full verification | Malformed, absent, cross-tenant, and policy-denied run identifiers share one not-found contract; policy dependency failure remains fail-closed and retryable |
| 2026-08-22 | RUN-005 | `6f3fa4b` | Typed signal validation; policy/HTTP/idempotency unit and race tests; PostgreSQL 18.4 atomic replay and concurrent ordered-event integration; OpenAPI lint; `make verify` | `PAUSE`, `RESUME`, and `CUSTOM` are durable commands rather than lifecycle transitions; `CANCEL` remains exclusive to RUN-006. Acceptance atomically commits signal, event, audit, and at-least-once outbox delivery |
| 2026-08-22 | RUN-006 | `1ef9ac1` | Dedicated cancellation validation, policy, HTTP/idempotency, and race tests; PostgreSQL 18.4 atomic evidence, tenant/version/replay, and terminal-race integration repeated 20 times; OpenAPI lint; `make verify` | Cancellation locks the run and invalidates its lease/fencing generation when it wins; an already-established terminal transition is returned unchanged without duplicate cancellation evidence |
| 2026-08-22 | RUN-007 | `45d8c14` | HMAC cursor/domain, authorization/application, SSE HTTP, retention, heartbeat, and backpressure unit/race tests; PostgreSQL 18.4 ordered resume and tenant-isolation integration; OpenAPI lint; `make verify` | Every connection reauthorizes through `runs.read`; cursors are run-bound positions, fresh streams begin at the retained floor, stale resumes return 410 before headers, and per-write deadlines bound slow clients |
| 2026-08-22 | RUN-008 | `289dd2f` | Worker lease/domain/application unit and race tests; PostgreSQL 18.4 expiry/reclaim, simultaneous claim, fencing, ordered-event, lifecycle, and stale-worker integration; `make verify` | Claims are tenant-queue scoped and use `SKIP LOCKED`; expiry equality is stale, every reclaim advances the fence, terminal changes clear the lease, and the outbox crash/replay expansion remains RUN-009 |
| 2026-08-22 | RUN-009 | `d0d88df` | Transactional worker event/outbox identity coverage; PostgreSQL 18.4 post-send crash and lease-expiry replay integration repeated 10 times; focused race suite; `make verify` | Every worker mutation emits an outbox row atomically; the run-event UUID is reused as the sink deduplication key, so ambiguous post-send crashes replay the same identity |
| 2026-08-22 | RUN-010 | `81b7508` | Mounted public HTTP/application/PostgreSQL lifecycle test; OpenAPI response-shape and lint checks; exact idempotency replay/conflict; cross-tenant isolation; SSE reconnect; terminal concurrency repeated 10 times under the race detector; complete end-to-end suite; `make verify` | One composed workflow now qualifies the Phase 4 public surface and trusted worker interaction; focused adapter suites remain the deeper source of crash/replay, retention, fencing, and high-contention coverage |
| 2026-08-22 | RUN-011 | `6518df7` | Phase 4 API/example/state-machine/evidence reconciliation; clean-tree `make verify` | Phase 4 exit qualified the public lifecycle and trusted worker boundary; resource dimensions, accounting, settlement, and reclaim remain Phase 5 |
| 2026-08-25 | RES-001 | `a1ada23` | Typed tenant-scoped dimension definitions and quantity validation; migration 009 domain/schema constraints; PostgreSQL 18.4 forward-upgrade qualification; domain unit/race tests; clean-tree `make verify` at `64a734b` | Dimensions remain data-driven within the three governed behavioral classes. Deadline-like values use absolute UTC Unix microseconds; adding a behavioral class or representation requires a forward migration rather than accepting an untyped value |
| 2026-08-25 | RES-002 | `43d04ee` | Exact and deterministic policy-to-dimension grant mapping; atomic immutable grant and mutable initial-balance persistence; PostgreSQL 18.4 bounds rollback and immutability checks; full integration/end-to-end suites; clean-tree `make verify` | Admission fails closed when a policy resource constraint has no matching tenant dimension or falls outside its configured bounds. Deadline stays in the immutable run admission record until deadline enforcement is implemented |
| 2026-08-26 | RES-003 | `8a37a87` | Typed positive reservation vectors; deterministic UUID lock ordering; checked parent debits plus child grant/balance initialization; PostgreSQL 18.4 atomic rollback and inverse-order concurrency repeated 5 times; clean-tree `make verify` | The persistence primitive governs consumables only and requires a child run/envelope already linked to its parent. RES-004 composes it with authorized child admission plus active/total-child and delegation-depth enforcement |
| 2026-08-26 | RES-004 | `996d48b` | Authorized child aggregate, parent link, topology checks, consumable reservation, and evidence composed under one transaction; parent-envelope lock serializes sibling admission; PostgreSQL 18.4 concurrent active ceiling, persistent total ceiling, depth, narrowing, and full rollback tests repeated 3 times; `make verify` | Active children are open reservations; total children are all durable reservations. Depth is evaluated from authoritative envelope ancestry, and every child structural grant must not exceed its parent's immutable grant |
| 2026-08-26 | RES-005 | `de85483` | Authenticated producer binding and `resources.meter` authorization; deterministic content digest and replay receipt; PostgreSQL 18.4 atomic ledger/balance application, mismatch and over-budget rollback; full integration suite; `make verify` | Usage is an incremental nonnegative consumable amount. Advisory transaction locks serialize previously unseen producer/source keys; PostgreSQL remains authoritative and late post-settlement usage fails closed |
| 2026-08-26 | RES-006 | `e4adb0f` | PostgreSQL-backed fixed UTC minute windows coupled atomically to metering; 10-way concurrent ceiling qualification repeated 3 times; integrity-protected Valkey exhausted markers, expiry, outage/corruption bypass; unit/race/integration suites; `make verify` | Throughput uses server recorded time and the structural `<resource>_per_minute` companion grant. Valkey can only repeat an authoritative denial; misses and failures use PostgreSQL, while duplicate source events replay before any cached block |
| 2026-08-26 | RES-007 | `534fdbe` | Atomic trusted-usage zero-balance detection; policy-selected pause/fail disposition; lease fencing; ordered lifecycle, audit, and outbox evidence; concurrent exact-exhaustion repeated 3 times against PostgreSQL 18.4; pinned OPA 1.19.0 20-case suite; `make verify` | Metering is accepted only for `RUNNING`, but identical source-event replay resolves first. Low/medium-risk baseline policy pauses for extension; high/critical risk and missing advice fail conservatively. Both lifecycle steps commit with the final usage and balance mutation |
| 2026-08-26 | RES-008 | `461281e` | Authenticated administrative HTTP and zero-TTL authoritative OPA authorization; immutable migration 013 ledger; PostgreSQL 18.4 atomic consumable credit, deadline movement, envelope/state versioning, paused-run resume, audit/run-event/outbox evidence, exact replay, and mismatched-key rejection; pinned OPA 1.19.0 21-case suite; `make verify` | Original grants and consumption remain immutable; effective grants are original plus ordered extension items. Approval references are mandatory, terminal runs fail closed, and requester self-extension is denied independently by policy, application, and repository |
| 2026-08-26 | RES-009 | `e95c5f1` | Authenticated trusted settlement authorization; immutable migration 014 ledger; PostgreSQL 18.4 authoritative consumption, atomic close/parent credit/evidence, exact and concurrent replay; full integration/end-to-end suites; `make verify` | Settlement requires the durable declared terminal state and no open descendant allocation. Optional final usage IDs are validated, but quantities derive only from authoritative balances; unique settlement and actor-scoped idempotency prevent double credit |
| 2026-08-26 | RES-010 | `44a247b` | Bounded tenant reconciliation service; PostgreSQL 18.4 terminal-first `SKIP LOCKED` claims, expiry timeout/fencing, authoritative multi-dimensional reclaim, forced late rollback, concurrent replicas, and replay repeated 3 times; unit/race/integration suites; `make verify` | Reconciliation requires an active tenant `SYSTEM` principal. Each candidate commits independently; open descendants, unexpired live children, and non-authoritative states retain allocation for a later safe pass |
| 2026-08-26 | RES-011 | `1d9adfc` | Randomized reservation/usage/leaf-settlement conservation model; checked signed 64-bit decimal arithmetic oracle; PostgreSQL 18.4 post-settlement conservation assertion; unit/race/integration suites; `make verify` | Property coverage found and corrected missing descendant-consumption roll-up during terminal settlement and reconciliation. Parent balances now retain `available + direct_consumed + allocated_open = effective_grant`; checked subtraction accepts valid `MinInt64 - MinInt64` while rejecting true overflow |
| 2026-08-26 | RES-012 | `014954a` | 32 synchronized contenders each for inverse-order multi-dimension reservation, same-command terminal settlement, and governed child admission; PostgreSQL 18.4 stress scenario repeated 5 times; focused race suite; `make verify` | Deterministic dimension locking completes without deadlock and permits only the capacity-backed reservation. Settlement idempotency produces one ledger/evidence set and one exact parent credit under retry pressure. Parent-envelope serialization admits exactly one child at an active-child ceiling of one, with every rejected aggregate rolled back |
| 2026-08-26 | RES-013 | `edf1dec` | Composed application services and PostgreSQL 18.4 end-to-end workflow; focused race qualification; complete end-to-end suite; `make verify` | A 60-token child exhausts and pauses, receives and consumes a governed 20-token extension, and settles 60 reserved tokens into parent consumption. A 40-token expired sibling is timed out and reclaimed, after which a later sibling reuses exactly the restored capacity |
| 2026-08-26 | RES-014 | `775539f` | Phase 5 resource/accounting and PostgreSQL schema reconciliation; randomized conservation properties; 32-way allocation/settlement/structural contention; composed PostgreSQL 18.4 lifecycle; clean-tree `make verify` | Phase 5 exit gate passed: authoritative locking, immutable ledgers, idempotent closure, and leaf-first reconciliation preserve capacity without oversubscription or double credit; revocation freshness remains Phase 6 |
| 2026-08-27 | REV-001–REV-002 | `d77dec1` | typed scope/time/reason validation; authenticated administrative HTTP and authoritative OPA authorization; atomic PostgreSQL change/epoch/log/audit/outbox transaction; 16-way monotonic concurrency repeated 3 times; `make verify` | Every change advances the global epoch; tenant-scoped changes also advance tenant revocation, tenant/policy changes advance policy, and agent/version changes advance agent epochs. Lift is append-only and a second lift conflicts without advancing state |
| 2026-08-27 | REV-003–REV-005 | `72ff9de` | authoritative scoped evaluation and applicable epochs; governed worker checks and claim filtering; authenticated HMAC-cursor SSE with bounded writes/heartbeat/retention/gap; delta/snapshot reconciliation and non-regressing persisted checkpoints; PostgreSQL 18.4 integration; clean-tree `make verify` | SSE cursors bind tenant, global sequence, and security epoch. Retention gaps reconcile instead of silently skipping history. Gateway identity is token-derived, snapshots are deterministic SHA-256 content addresses, and stale replicas cannot regress durable checkpoints |
| 2026-08-27 | REV-006 | `8409b99` | tenant-scoped instance-local sequence/epoch, stream-receipt, reconciliation, and process-monotonic age tracking; concurrent, sparse-vector, regression, restart-default, and wall-clock-adjustment unit/race coverage; `make verify` | UTC timestamps are diagnostic only. Freshness starts unknown after every process restart; sparse event epochs merge into the applied high-water vector, while sequence, authoritative epoch, or monotonic-clock regression fails closed |
| 2026-08-27 | REV-007 | `2398385` | action/risk classifier; tenant tracker-derived policy state; unknown/restart/stale/clock-regression fail-closed tests; high-risk authoritative cache bypass and zero-TTL enforcement; operation-bound epoch/version cache keys and TTL; pinned OPA 1.19.0 24-case suite; `make verify` | Normal writes cannot exceed 30 seconds and sensitive reads cannot exceed 60 seconds; both may be tightened. Low-risk reads require an explicit finite bound. Unknown actions, governance/resource writes, and high/critical-risk workload writes conservatively require live authority |
| 2026-08-27 | REV-008 | `c7856ff` | bounded 1,024-entry process-local cache; tenant/global generation-bound hashed Valkey keys; immediate committed revocation, accepted stream/reconciliation, and activation/rollback invalidation; forged/stale/global/tenant invalidation tests; PostgreSQL 18.4 integration; clean-tree `make verify` at `13abde8` | Old Valkey entries are not enumerated or synchronously deleted: policy digest/version, revocation epochs, and local invalidation generations make them unreachable until their existing bounded TTL expires. Global changes advance an independent generation so every tenant namespace misses immediately; Valkey availability is never required to invalidate local authority |
| 2026-08-27 | REV-009 | `90f913a` | aggregate scrape-time age, authoritative-sequence lag, current-gap, gap-incident, tracked-tenant, and freshness-health metrics; readiness and HTTP composition tests; focused unit/race plus clean-tree `make verify` | Tenant identifiers are never metric labels. A tracked tenant is not ready until authority is observed after restart, and remains not ready while stale, lagging, gapped, or monotonic-clock unhealthy; reconciliation clears a gap only with its applied authoritative state, while liveness remains independent |
| 2026-08-27 | REV-010 | `bf8eae1` | dropped-sequence detection; duplicate/out-of-order/restart/partition/cache-expiry/reconciliation/global-revocation coverage; focused unit/race suites; PostgreSQL 18.4 integration and e2e; pinned OPA 1.19.0 24-case suite; clean-tree `make verify` | Exact duplicate stream events are idempotent but cannot refresh authority. A missing sequence preserves the last fully applied state and immediately denies bounded authorization; only authoritative reconciliation clears the gap, advances epochs, invalidates stale cache reachability, and restores serving |
| 2026-08-27 | REV-011 | `57888c2` | 100 authoritative PostgreSQL changes measured through authenticated HTTP SSE at p50 10.239 ms, p99 22.463 ms, and max 33.979 ms against the connected-gateway p99 <= 5 s objective; repeated measurement, Phase 6 protocol/evidence reconciliation, clean-tree `make verify` | Gateway local state and its fully applied cursor form one durable boundary; service checkpoints are operational progress, not proof of apply. Gap/retention/decode failures reconcile before serving resumes. Phase 8 retains 5,000-client fanout, reconnect-storm, cross-zone, and sustained-capacity qualification |
| 2026-09-01 | SEC-006 | pending implementation commit | authenticated HTTPS export; deterministic sink-specific hash chain and event-ID replay; strict bound receipt; PostgreSQL lease takeover, stale-writer fencing, append-only receipt, and atomic checkpoint integration; focused and aggregate verification | Bearer authentication is the implemented adapter boundary; workload identity and mTLS remain SEC-008. Sink unavailability retains the authoritative outbox and prevents checkpoint advancement without rolling back governance state |
| 2026-09-01 | SEC-007 | `acc08c6` | closed v1 emergency scope; recent phishing-resistant MFA verification; digest-bound approval consumption; fifteen-minute grant and digest-only credential; append-only activation/use/expiry/revocation with atomic export; focused and aggregate verification | Break-glass is not a role and cannot expand Run authority. Only exact policy or revocation recovery resources are eligible; every use rechecks identity, credential, resource, expiry, and terminal state |
| 2026-09-01 | SEC-008 | `6d7771a` | closed v1 URI-SAN binding schema; replaceable verified-chain mTLS mapper; workload-only trusted-route middleware; principal-type and action-specific policy enforcement; focused and aggregate verification | Production requires verified client certificates and exact deployment-owned bindings. Local/test transport exceptions must inject fixed allowlisted identities and never trust headers, addresses, arbitrary bearer claims, or request bodies |
| 2026-09-01 | SEC-009 | `a7790e6` | sentinel leak tests for structured logs/errors, trace names/options/attributes/events/errors, metric labels, client problems, policy input, audit metadata/headers, request paths, and panic recovery; focused and aggregate verification | Error values are fully redacted in logs; traces expose only stable names and allowlisted attributes; restricted audit/header and policy-input fields fail closed; idempotency and reconciliation keys were removed from audit metadata |
| 2026-09-02 | OPS-005 | `e528009`, `01135d6` | policy/revocation reconciliation unit and race tests; active-policy plus sequence/epoch PostgreSQL integration; process-only liveness and fail-closed readiness smoke; clean-tree `make verify` against an isolated PostgreSQL database | Production readiness now requires database health, a currently valid loaded active policy, and authoritative revocation state no older than the 30-second normal-write bound. Reconciliation uses one bounded metadata query and recovers without tying dependency outages to liveness |
| 2026-09-02 | OPS-006 | `41ab200` | operational publisher and bounded-series unit/race tests; real PostgreSQL aggregate snapshot integration; optional HPA/ServiceMonitor and dashboard contract checks; clean-tree `make verify` against an isolated PostgreSQL database | Production publishes outbox count/age and terminal settlement lag every 15 seconds and pgx pool saturation at scrape time. Policy/allocation/admission/cache outcome series use closed labels and exist at zero before their first event; collection failure is non-authoritative and does not alter readiness |
| 2026-09-02 | OPS-007 | `bd203e5` | 27 Prometheus alerts parsed by native Prometheus 3.5 `promtool`; optional Kustomize render; actionable-alert contract test; metrics unit/race tests; clean-tree `make verify` against an isolated PostgreSQL database | Availability is split into public-read, run-mutation, and admin/trusted budgets with fast/slow windows. API and OPA p95/p99, dependency health, reconciliation, contention, outbox, and settlement alerts use bounded labels, sustained anti-noise windows, severity/ownership, and runbook links. This closes gaps in the earlier foundation rule set without changing SLO targets |
| 2026-09-02 | OPS-009 | `fa04a0c` | pinned PostgreSQL 18.4 physical base backup with continuous WAL archive; ephemeral AES-256 backup encryption; named PITR before/after an atomic governance transaction; exact epoch, audit/outbox, and allocation assertions; schema 17-to-18 forward migration; restored invariant check; focused test and clean-tree `make verify` | Both selected transaction boundaries recovered exactly with zero committed-transaction loss relative to the target. Local observed RTO was 3–4 seconds; it is rehearsal evidence, not a production-provider latency claim. Provider backup identifiers, KMS references, and production RTO/RPO remain game-day records outside the repository |

## Active blockers and deviations

- ENG-005 inherits eight exact pseudo-version dependencies from the pinned Prometheus/OpenTelemetry graph. Exceptions are version-bound in `dependency-policy.json`, expire 2027-02-10, and must be removed or renewed through review before that date.
- DATA-001 adds exact pseudo-version dependencies for pgx's `pgservicefile` and Tern's `go-ini`. Exceptions are version-bound in `dependency-policy.json`, expire 2027-02-19, and must be removed or renewed through review before that date.
